use std;

fn multiply(a, b) {
    let result;
    for let i = 0; i < b; i = i + 1; {
        result = result + a;
    }

    return result;
}

fn _main() {
    let a = input();
    let b = input();
    println(multiply(a, b));
}