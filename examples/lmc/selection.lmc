fn main() {
    let score = input();
    let grade;

    if (score >= 75) {
        grade = 'A';
    } elif (score >= 50) {
        grade = 'B';
    } else {
        grade = 'C';
    }

    printc(grade);
}