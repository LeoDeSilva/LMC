use std;

fn double(x) {
    return x+x;
}

fn _main() {
    let score = input();
    let grade;

    if score > 50 {
        grade = 66;
    } elif score > 75 {
        grade = 65;
    } else {
        grade = 67;
    }

    printc(grade);

    return 0;
}